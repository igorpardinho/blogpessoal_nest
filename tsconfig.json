{
  "compilerOptions": {
    "target": "ES2023",                          // Usa features modernas do JS
    "module": "commonjs",                        // Comum em Node.js, ajuste se usar ES modules
    "declaration": true,                         // Gera arquivos .d.ts para tipagem externa
    "removeComments": true,                      // Remove comentários do output
    "emitDecoratorMetadata": true,               // Necessário para decoradores (NestJS)
    "experimentalDecorators": true,               // Habilita decoradores
    "allowSyntheticDefaultImports": true,        // Permite import default de módulos comuns
    "sourceMap": true,                            // Gera source maps para debugging
    "outDir": "./dist",                           // Diretório de saída compilado
    "baseUrl": "./",                              // Base para imports relativos
    "incremental": true,                          // Incremental build (mais rápido)
    "skipLibCheck": true,                         // Pula checagem de tipos em libs para acelerar build
    "strict": false,                               // Habilita todas as verificações estritas
    "strictNullChecks": true,                      // (redundante, mas explícito) null/undefined
    "forceConsistentCasingInFileNames": true,    // Sensível a maiúsculas/minúsculas
    "noImplicitAny": true,                        // Pode ser true para mais rigor, mas false aqui
    "strictBindCallApply": true,                   // Mais segurança no uso de bind/call/apply
    "noFallthroughCasesInSwitch": true            // Evita fallthrough em switch (erro)
  },
  "exclude": ["node_modules", "dist"]
}
